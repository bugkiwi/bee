# Sample Task: Hello World CLI

Create a simple Bun TypeScript CLI that prints "Hello, World!" to stdout.

## Requirements
- File: src/hello.ts
- Command: `bun src/hello.ts` outputs "Hello, World!"
- Has at least one test in src/hello.test.ts
